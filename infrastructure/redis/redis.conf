# Redis configuration for AI Finance Agency
# Basic configuration
appendonly yes
appendfsync everysec

# Memory management
maxmemory-policy allkeys-lru

# Network
bind 0.0.0.0
port 6379

# Logging
loglevel notice

# Security (no password for development)
# requirepass will be set via command line if needed